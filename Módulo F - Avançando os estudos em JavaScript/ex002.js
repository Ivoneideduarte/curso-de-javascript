let valores = [8 , 1, 7, 4, 2, 9]

for(let pos=0; pos < valores.length; pos++) { // Inicia na posição zero e vai até o comprimento do vetor
    console.log(`A posição ${pos} tem o valor ${valores[pos]}`)
}